// @TODO: import action types here

const initialState = {
};

function rootReducer(state = initialState, action) {
    
    // @TODO: depending on action type, change state
    // @TODO: return new state if something has changed

    return state;
};

export default rootReducer;